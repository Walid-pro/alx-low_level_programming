#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result.
 * @a: first integer.
 * @b: second integer..
 * Return: result of addition.
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
